<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FirstQA - Your AI QA Engineer</title>
    <meta name="description" content="Meet Ovi, your AI QA Engineer. Reviews requirements, analyzes PRs, tests on staging, files bugs, automates regressions, and gives go/no-go on releases. The full QA role, powered by AI.">
    
    <link rel="icon" type="image/svg+xml" href="/logos/firstqa-favicon.svg">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>

<!-- Early Access Modal -->
<div class="modal-overlay" id="ea-modal">
    <div class="modal-card">
        <button class="modal-close" id="modal-close">&times;</button>
        <h2>Get Early Access</h2>
        <p>Give your team a QA engineer today. Early access teams get founder-direct support and help shape Ovi&rsquo;s roadmap.</p>
        <form class="ea-form" id="subscribe-form" action="/subscribe" method="POST">
            <div class="ea-input-group">
                <input type="email" name="email" placeholder="you@company.com" required>
                <button type="submit" class="btn-submit">
                    <span>Get Early Access</span>
                    <i class="fas fa-arrow-right"></i>
                </button>
            </div>
            <p class="ea-message" id="subscribe-message"></p>
        </form>
        <p class="ea-demo-link">Prefer a walkthrough? <a href="https://calendly.com/firstqa/demo" target="_blank">Book a call</a></p>
    </div>
</div>

<!-- Navigation -->
<nav class="navbar">
    <div class="container">
        <a href="/" class="logo">
            <svg class="logo-icon-svg" width="28" height="28" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
                <defs>
                    <linearGradient id="logoGradNav" x1="0%" y1="0%" x2="100%" y2="100%">
                        <stop offset="0%" style="stop-color:#0d9488;stop-opacity:1" />
                        <stop offset="100%" style="stop-color:#059669;stop-opacity:1" />
                    </linearGradient>
                </defs>
                <circle cx="16" cy="16" r="14" fill="url(#logoGradNav)"/>
                <path d="M10 16 L14 20 L22 11" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
            </svg>
            <span class="logo-text">FirstQA</span>
        </a>
        <div class="nav-menu">
            <a href="#features" class="nav-link">What Ovi Does</a>
            <a href="#how-it-works" class="nav-link">How It Works</a>
            <a href="/docs" class="nav-link">Docs</a>
            <a href="#" class="btn-signup" data-ea-trigger>Get Early Access</a>
        </div>
        <button class="mobile-menu-btn" aria-label="Menu">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </div>
</nav>

<!-- Hero -->
<section class="hero">
    <div class="hero-bg">
        <div class="grid-pattern"></div>
        <div class="glow glow-1"></div>
        <div class="glow glow-2"></div>
    </div>
    <div class="container">
        <div class="hero-content">
            <div class="hero-badge">
                <span class="pulse"></span>
                Now in Early Access
            </div>
            <h1>
                Meet <span class="highlight">Ovi</span>, Your<br>
                AI QA Engineer
            </h1>
            <p class="hero-sub">
                Ovi joins your team and workflow instantly. From reviewing requirements 
                to giving the go/no-go on releases &mdash; the full QA role, powered by AI.
            </p>
            <div class="hero-cta">
                <a href="#" class="btn-primary" data-ea-trigger>
                    <span>Get Early Access</span>
                    <i class="fas fa-arrow-right"></i>
                </a>
                <a href="#how-it-works" class="btn-ghost">
                    See How It Works
                </a>
            </div>
            <div class="hero-stats">
                <div class="stat">
                    <span class="stat-value">5</span>
                    <span class="stat-label">QA Pillars Covered</span>
                </div>
                <div class="stat-divider"></div>
                <div class="stat">
                    <span class="stat-value">&lt;2min</span>
                    <span class="stat-label">Setup Time</span>
                </div>
                <div class="stat-divider"></div>
                <div class="stat">
                    <span class="stat-value">24/7</span>
                    <span class="stat-label">Always On</span>
                </div>
            </div>
        </div>
        <div class="hero-visual">
            <div class="terminal">
                <div class="terminal-header">
                    <div class="terminal-dots">
                        <span></span><span></span><span></span>
                    </div>
                    <span class="terminal-title">ovi-qa-engineer</span>
                </div>
                <div class="terminal-body">
                    <div class="terminal-line">
                        <span class="prompt">$</span>
                        <span class="command">@ovi review PR #142</span>
                    </div>
                    <div class="terminal-output">
                        <div class="output-line fade-in-1">
                            <span class="icon">&#128270;</span> Analyzing changes &amp; requirements...
                        </div>
                        <div class="output-line fade-in-2">
                            <span class="icon">&#9888;&#65039;</span> 2 gaps flagged in acceptance criteria
                        </div>
                        <div class="output-line fade-in-3">
                            <span class="icon">&#128203;</span> Generated 8 test cases
                        </div>
                        <div class="output-line fade-in-4">
                            <span class="icon">&#127917;</span> Testing on staging...
                        </div>
                        <div class="output-line fade-in-5">
                            <span class="icon">&#9989;</span> 7 passed <span class="warning">&bull; 1 bug filed</span>
                        </div>
                        <div class="output-line fade-in-6">
                            <span class="icon">&#128221;</span> QA review posted to PR
                        </div>
                        <div class="output-line fade-in-7">
                            <span class="icon">&#128640;</span> Release v2.4 &rarr; <span class="go-badge">Go</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- What Ovi Does -->
<section id="features" class="features">
    <div class="container">
        <div class="section-header">
            <span class="section-tag">The Complete QA Engineer</span>
            <h2>Everything a QA Engineer Does A-Z.</h2>
            <p>Ovi handles the complete QA workflow &mdash; from ticket to production.</p>
        </div>
        
        <div class="pillars-grid">
            <div class="pillar-card">
                <div class="pillar-icon">
                    <i class="fas fa-clipboard-check"></i>
                </div>
                <h3>Requirements &amp; Design Review</h3>
                <p>Flags gaps in requirements and design before development starts. Asks the questions a senior QA would.</p>
            </div>
            
            <div class="pillar-card">
                <div class="pillar-icon">
                    <i class="fas fa-list-check"></i>
                </div>
                <h3>Test Recipe Creation</h3>
                <p>Generates test cases from tickets and designs. Your team&rsquo;s shared definition of &ldquo;done&rdquo; and testing playbook.</p>
            </div>
            
            <div class="pillar-card">
                <div class="pillar-icon">
                    <i class="fas fa-code-pull-request"></i>
                </div>
                <h3>PR Review (QA Lens)</h3>
                <p>Reviews PRs for risks, logic gaps, and testability. Gives devs test cases to verify their own work.</p>
            </div>
            
            <div class="pillar-card">
                <div class="pillar-icon">
                    <i class="fas fa-hand-pointer"></i>
                </div>
                <h3>Manual Testing on Staging</h3>
                <p>Tests features in the browser like a real QA would. Explores edge cases, files bugs with repro steps.</p>
            </div>
            
            <div class="pillar-card">
                <div class="pillar-icon">
                    <i class="fas fa-robot"></i>
                </div>
                <h3>Test Automation</h3>
                <p>Automates critical regressions with Playwright. Living test suites that evolve with your product.</p>
            </div>
        </div>
    </div>
</section>

<!-- How It Works -->
<section id="how-it-works" class="how-it-works">
    <div class="container">
        <div class="section-header">
            <span class="section-tag">Onboard Ovi Like a New Hire</span>
            <h2>First Day. Already Contributing.</h2>
        </div>
        
        <div class="steps-container">
            <div class="step">
                <div class="step-number">1</div>
                <div class="step-content">
                    <h3>Give Ovi Access</h3>
                    <p>Connect your tools. Ovi learns your codebase, indexes your repo, and builds full context on your product. Set up in minutes, not days.</p>
                    <div class="step-tools">
                        <span class="tool-badge"><i class="fab fa-github"></i> GitHub</span>
                        <span class="tool-badge">Linear</span>
                        <span class="tool-badge"><i class="fab fa-slack"></i> Slack</span>
                    </div>
                </div>
            </div>
            
            <div class="step-connector">
                <svg viewBox="0 0 24 80" fill="none">
                    <path d="M12 0 L12 80" stroke="currentColor" stroke-width="2" stroke-dasharray="4 4"/>
                </svg>
            </div>
            
            <div class="step">
                <div class="step-number">2</div>
                <div class="step-content">
                    <h3>Tag Ovi Where You Need Quality</h3>
                    <p>Mention @ovi in Slack, tag on Linear issues, or let Ovi watch your GitHub repos. Ovi joins your workflow &mdash; you don&rsquo;t change yours.</p>
                </div>
            </div>
            
            <div class="step-connector">
                <svg viewBox="0 0 24 80" fill="none">
                    <path d="M12 0 L12 80" stroke="currentColor" stroke-width="2" stroke-dasharray="4 4"/>
                </svg>
            </div>
            
            <div class="step">
                <div class="step-number">3</div>
                <div class="step-content">
                    <h3>Ovi Does the QA Work</h3>
                    <p>Requirements reviewed. PRs analyzed. Staging tested. Bugs filed. Regressions automated. Release decision made. Every sprint, every day.</p>
                </div>
            </div>
        </div>

        <div class="section-cta">
            <a href="#" class="btn-primary" data-ea-trigger>
                <span>Get Early Access</span>
                <i class="fas fa-arrow-right"></i>
            </a>
        </div>
    </div>
</section>

<!-- Why Section -->
<section class="why-section">
    <div class="container">
        <div class="section-header">
            <span class="section-tag">The Real Problem</span>
            <h2>Moving Fast &mdash; But Paying a <span class="highlight">Hidden Tax</span></h2>
            <p>Developers test like builders, not users. They know the system too well, miss edge cases, and skip the &ldquo;stupid user&rdquo; scenarios. The result is a velocity tax that silently kills momentum.</p>
        </div>

        <div class="tax-grid">
            <div class="tax-item">
                <div class="tax-icon"><i class="fas fa-rotate-left"></i></div>
                <div class="tax-text">
                    <strong>30-40% rework</strong>
                    <span>Sprint capacity lost to bugs and fixes</span>
                </div>
            </div>
            <div class="tax-item">
                <div class="tax-icon"><i class="fas fa-shuffle"></i></div>
                <div class="tax-text">
                    <strong>Constant context-switching</strong>
                    <span>Building new features vs. fixing old ones</span>
                </div>
            </div>
            <div class="tax-item">
                <div class="tax-icon"><i class="fas fa-bug"></i></div>
                <div class="tax-text">
                    <strong>Bugs in production</strong>
                    <span>Found by users, not caught on staging</span>
                </div>
            </div>
            <div class="tax-item">
                <div class="tax-icon"><i class="fas fa-dice"></i></div>
                <div class="tax-text">
                    <strong>Releases feel like a gamble</strong>
                    <span>No one really knows what will break</span>
                </div>
            </div>
        </div>

        <div class="why-solution">
            <p><strong>The first QA hire should change that.</strong> Catch gaps before development. Review PRs through a quality lens. Test on staging. Automate regressions. Give the go/no-go on releases.</p>
            <p class="why-closer"><strong>That&rsquo;s what Ovi does. From day one.</strong></p>
        </div>

        <div class="why-comparison">
            <div class="comparison">
                <div class="comparison-item bad">
                    <div class="comparison-header">
                        <span class="comparison-icon">&#10007;</span>
                        <span>Without QA</span>
                    </div>
                    <ul>
                        <li><i class="fas fa-times"></i> Devs test their own code (blind spots)</li>
                        <li><i class="fas fa-times"></i> Bugs found in production</li>
                        <li><i class="fas fa-times"></i> No requirements review</li>
                        <li><i class="fas fa-times"></i> Testing is an afterthought</li>
                        <li><i class="fas fa-times"></i> Releases feel like a gamble</li>
                    </ul>
                </div>
                <div class="comparison-item good">
                    <div class="comparison-header">
                        <svg class="comparison-logo" width="22" height="22" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
                            <defs>
                                <linearGradient id="logoGradComp" x1="0%" y1="0%" x2="100%" y2="100%">
                                    <stop offset="0%" style="stop-color:#0d9488;stop-opacity:1" />
                                    <stop offset="100%" style="stop-color:#059669;stop-opacity:1" />
                                </linearGradient>
                            </defs>
                            <circle cx="16" cy="16" r="14" fill="url(#logoGradComp)"/>
                            <path d="M10 16 L14 20 L22 11" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
                        </svg>
                        <span>With Ovi</span>
                    </div>
                    <ul>
                        <li><i class="fas fa-check"></i> Every ticket reviewed before dev starts</li>
                        <li><i class="fas fa-check"></i> Every PR analyzed through a QA lens</li>
                        <li><i class="fas fa-check"></i> Features tested on staging first</li>
                        <li><i class="fas fa-check"></i> Engineers empowered to test their own work</li>
                        <li><i class="fas fa-check"></i> Release decisions based on evidence</li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="section-cta" style="margin-top: 64px;">
            <a href="#" class="btn-primary" data-ea-trigger>
                <span>Get Early Access</span>
                <i class="fas fa-arrow-right"></i>
            </a>
        </div>
    </div>
</section>

<!-- Human QA On Demand -->
<section class="human-qa">
    <div class="container">
        <div class="human-qa-card">
            <div class="human-qa-icon">
                <i class="fas fa-user-check"></i>
            </div>
            <div class="human-qa-content">
                <span class="human-qa-tag">When You Need Human Expertise</span>
                <h3>On-Demand Human QA</h3>
                <p>
                    Ovi covers day-to-day QA. But for complex exploratory testing, UX deep dives, 
                    or pre-launch audits &mdash; FirstQA provides senior human QA engineers on demand. 
                    No contracts, no commitments. They work alongside Ovi.
                </p>
                <ul class="human-qa-list">
                    <li><i class="fas fa-check"></i> Exploratory testing before major releases</li>
                    <li><i class="fas fa-check"></i> Complex user flows that need human intuition</li>
                    <li><i class="fas fa-check"></i> Security &amp; edge case deep dives</li>
                    <li><i class="fas fa-check"></i> Seamlessly complements Ovi&rsquo;s coverage</li>
                </ul>
                <a href="https://calendly.com/firstqa/demo" class="btn-human-qa" target="_blank">
                    <span>Talk to Us About Human QA</span>
                    <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Bottom CTA -->
<section class="bottom-cta">
    <div class="container">
        <div class="bottom-cta-content">
            <h2>Ready to Give Your Team a QA Engineer?</h2>
            <p>Join early access and bring Ovi onto your team today.</p>
            <a href="#" class="btn-primary btn-large" data-ea-trigger>
                <span>Get Early Access</span>
                <i class="fas fa-arrow-right"></i>
            </a>
        </div>
    </div>
</section>

<!-- Footer -->
<footer class="footer">
    <div class="container">
        <div class="footer-main">
            <div class="footer-brand">
                <a href="/" class="logo">
                    <svg class="logo-icon-svg" width="28" height="28" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
                        <defs>
                            <linearGradient id="logoGradFoot" x1="0%" y1="0%" x2="100%" y2="100%">
                                <stop offset="0%" style="stop-color:#0d9488;stop-opacity:1" />
                                <stop offset="100%" style="stop-color:#059669;stop-opacity:1" />
                            </linearGradient>
                        </defs>
                        <circle cx="16" cy="16" r="14" fill="url(#logoGradFoot)"/>
                        <path d="M10 16 L14 20 L22 11" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
                    </svg>
                    <span class="logo-text">FirstQA</span>
                </a>
                <p>Your AI QA Engineer. Quality across the entire SDLC.</p>
            </div>
            <div class="footer-links">
                <div class="footer-col">
                    <h4>Product</h4>
                    <a href="#features">What Ovi Does</a>
                    <a href="#how-it-works">How It Works</a>
                    <a href="/docs">Documentation</a>
                </div>
                <div class="footer-col">
                    <h4>Company</h4>
                    <a href="/about">About</a>
                    <a href="/contact">Contact</a>
                    <a href="https://github.com/ovidon83/firstqa" target="_blank">GitHub</a>
                </div>
                <div class="footer-col">
                    <h4>Legal</h4>
                    <a href="/privacy">Privacy</a>
                    <a href="/terms">Terms</a>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2026 FirstQA. All rights reserved.</p>
        </div>
    </div>
</footer>

<style>
/* ============================================
   CSS Variables & Reset
   ============================================ */
:root {
    --bg-dark: #0a0a0b;
    --bg-card: #111113;
    --bg-elevated: #18181b;
    --border: #27272a;
    --text-primary: #fafafa;
    --text-secondary: #a1a1aa;
    --text-muted: #71717a;
    --accent: #22d3ee;
    --accent-hover: #06b6d4;
    --accent-glow: rgba(34, 211, 238, 0.15);
    --success: #4ade80;
    --warning: #fbbf24;
    --danger: #f87171;
    --gradient: linear-gradient(135deg, #22d3ee 0%, #a78bfa 100%);
    --font-main: 'Space Grotesk', -apple-system, sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: var(--font-main);
    background: var(--bg-dark);
    color: var(--text-primary);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
}

/* ============================================
   Modal
   ============================================ */
.modal-overlay {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 200;
    background: rgba(0, 0, 0, 0.75);
    backdrop-filter: blur(6px);
    align-items: center;
    justify-content: center;
}

.modal-overlay.active {
    display: flex;
}

.modal-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 48px;
    max-width: 520px;
    width: 90%;
    position: relative;
    text-align: center;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
    animation: modalIn 0.25s ease-out;
}

@keyframes modalIn {
    from {
        opacity: 0;
        transform: translateY(16px) scale(0.97);
    }
    to {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}

.modal-close {
    position: absolute;
    top: 16px;
    right: 20px;
    background: none;
    border: none;
    color: var(--text-muted);
    font-size: 28px;
    cursor: pointer;
    transition: color 0.2s;
    line-height: 1;
}

.modal-close:hover {
    color: var(--text-primary);
}

.modal-card h2 {
    font-size: 28px;
    font-weight: 700;
    margin-bottom: 12px;
    letter-spacing: -0.5px;
}

.modal-card > p {
    font-size: 15px;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 32px;
    max-width: 400px;
    margin-left: auto;
    margin-right: auto;
}

/* ============================================
   Navigation
   ============================================ */
.navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 16px 0;
    background: rgba(10, 10, 11, 0.8);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
}

.navbar .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    color: var(--text-primary);
}

.logo-icon-svg {
    flex-shrink: 0;
}

.logo-text {
    font-size: 20px;
    font-weight: 700;
    letter-spacing: -0.5px;
}

.nav-menu {
    display: flex;
    align-items: center;
    gap: 32px;
}

.nav-link {
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 14px;
    font-weight: 500;
    transition: color 0.2s;
}

.nav-link:hover {
    color: var(--text-primary);
}

.btn-signup {
    background: var(--text-primary);
    color: var(--bg-dark);
    text-decoration: none;
    font-size: 14px;
    font-weight: 600;
    padding: 10px 20px;
    border-radius: 8px;
    transition: all 0.2s;
    cursor: pointer;
}

.btn-signup:hover {
    background: var(--accent);
    color: var(--bg-dark);
    transform: translateY(-1px);
}

.mobile-menu-btn {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
}

.mobile-menu-btn span {
    display: block;
    width: 24px;
    height: 2px;
    background: var(--text-primary);
    transition: 0.2s;
}

/* ============================================
   Hero Section
   ============================================ */
.hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 100px 0 60px;
    overflow: hidden;
}

.hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
}

.grid-pattern {
    position: absolute;
    inset: 0;
    background-image: 
        linear-gradient(var(--border) 1px, transparent 1px),
        linear-gradient(90deg, var(--border) 1px, transparent 1px);
    background-size: 60px 60px;
    opacity: 0.3;
    mask-image: radial-gradient(ellipse at center, black 0%, transparent 70%);
}

.glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.4;
}

.glow-1 {
    top: 10%;
    left: 20%;
    width: 400px;
    height: 400px;
    background: var(--accent);
}

.glow-2 {
    bottom: 20%;
    right: 10%;
    width: 300px;
    height: 300px;
    background: #a78bfa;
}

.hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
    padding-top: 40px;
    position: relative;
    z-index: 1;
}

.hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--accent-glow);
    border: 1px solid var(--accent);
    color: var(--accent);
    padding: 8px 16px;
    border-radius: 100px;
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 24px;
}

.pulse {
    width: 8px;
    height: 8px;
    background: var(--accent);
    border-radius: 50%;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.2); }
}

.hero h1 {
    font-size: 64px;
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -2px;
    margin-bottom: 24px;
}

.highlight {
    background: var(--gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.hero-sub {
    font-size: 18px;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 32px;
    max-width: 500px;
}

.hero-sub strong {
    color: var(--text-primary);
}

.hero-cta {
    display: flex;
    gap: 16px;
    margin-bottom: 48px;
}

.btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: var(--gradient);
    color: var(--bg-dark);
    font-size: 15px;
    font-weight: 600;
    padding: 14px 28px;
    border-radius: 10px;
    text-decoration: none;
    transition: all 0.3s;
    border: none;
    cursor: pointer;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px var(--accent-glow);
}

.btn-primary i {
    font-size: 12px;
    transition: transform 0.3s;
}

.btn-primary:hover i {
    transform: translateX(4px);
}

.btn-primary.btn-large {
    font-size: 17px;
    padding: 18px 36px;
}

.btn-ghost {
    display: inline-flex;
    align-items: center;
    color: var(--text-secondary);
    font-size: 15px;
    font-weight: 500;
    padding: 14px 24px;
    text-decoration: none;
    transition: color 0.2s;
}

.btn-ghost:hover {
    color: var(--text-primary);
}

.hero-stats {
    display: flex;
    align-items: center;
    gap: 32px;
}

.stat {
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.stat-value {
    font-size: 24px;
    font-weight: 700;
    color: var(--text-primary);
}

.stat-label {
    font-size: 12px;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 1px;
}

.stat-divider {
    width: 1px;
    height: 40px;
    background: var(--border);
}

/* Terminal Visual */
.hero-visual {
    display: flex;
    justify-content: center;
}

.terminal {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
    width: 100%;
    max-width: 540px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
}

.terminal-header {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 16px;
    background: var(--bg-elevated);
    border-bottom: 1px solid var(--border);
}

.terminal-dots {
    display: flex;
    gap: 6px;
}

.terminal-dots span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--border);
}

.terminal-dots span:nth-child(1) { background: #f87171; }
.terminal-dots span:nth-child(2) { background: #fbbf24; }
.terminal-dots span:nth-child(3) { background: #4ade80; }

.terminal-title {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-muted);
}

.terminal-body {
    padding: 24px;
    font-family: var(--font-mono);
    font-size: 13.5px;
}

.terminal-line {
    margin-bottom: 16px;
}

.prompt {
    color: var(--accent);
    margin-right: 8px;
}

.command {
    color: var(--text-primary);
}

.terminal-output {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.output-line {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--text-secondary);
    opacity: 0;
    animation: fadeIn 0.5s forwards;
}

.fade-in-1 { animation-delay: 0.5s; }
.fade-in-2 { animation-delay: 1.1s; }
.fade-in-3 { animation-delay: 1.7s; }
.fade-in-4 { animation-delay: 2.3s; }
.fade-in-5 { animation-delay: 2.9s; }
.fade-in-6 { animation-delay: 3.5s; }
.fade-in-7 { animation-delay: 4.1s; }

@keyframes fadeIn {
    to { opacity: 1; }
}

.output-line .icon {
    font-size: 15px;
}

.go-badge {
    display: inline-block;
    background: rgba(74, 222, 128, 0.15);
    color: var(--success);
    font-weight: 600;
    padding: 1px 8px;
    border-radius: 4px;
    font-size: 12px;
}

.output-line .success {
    color: var(--success);
    font-weight: 600;
}

.output-line .warning {
    color: var(--warning);
}

.output-line .link {
    color: var(--accent);
    text-decoration: underline;
    cursor: pointer;
}

/* ============================================
   Features / Pillars Section
   ============================================ */
.features {
    padding: 120px 0;
    background: var(--bg-card);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
}

.section-header {
    text-align: center;
    margin-bottom: 64px;
}

.section-tag {
    display: inline-block;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--accent);
    margin-bottom: 16px;
}

.section-header h2 {
    font-size: 42px;
    font-weight: 700;
    letter-spacing: -1px;
    margin-bottom: 16px;
}

.section-header p {
    font-size: 18px;
    color: var(--text-secondary);
    max-width: 640px;
    margin: 0 auto;
    line-height: 1.7;
}

.pillars-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: center;
}

.pillar-card {
    background: var(--bg-dark);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 32px 28px;
    position: relative;
    transition: all 0.3s;
    width: calc(33.333% - 14px);
}

.pillar-card:hover {
    border-color: var(--accent);
    transform: translateY(-4px);
    background: linear-gradient(180deg, rgba(34, 211, 238, 0.06) 0%, transparent 60%);
    box-shadow: 0 12px 40px rgba(34, 211, 238, 0.06);
}

.pillar-icon {
    width: 52px;
    height: 52px;
    background: var(--accent-glow);
    border: 1px solid rgba(34, 211, 238, 0.2);
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 24px;
}

.pillar-icon i {
    font-size: 21px;
    color: var(--accent);
}

.pillar-card:hover .pillar-icon {
    background: rgba(34, 211, 238, 0.2);
    border-color: var(--accent);
}

.pillar-card h3 {
    font-size: 18px;
    font-weight: 600;
    margin-bottom: 10px;
}

.pillar-card p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.65;
}

/* ============================================
   How It Works
   ============================================ */
.how-it-works {
    padding: 120px 0;
}

.steps-container {
    max-width: 600px;
    margin: 0 auto;
}

.step {
    display: flex;
    gap: 24px;
    align-items: flex-start;
}

.step-number {
    width: 48px;
    height: 48px;
    background: var(--gradient);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    font-weight: 700;
    color: var(--bg-dark);
    flex-shrink: 0;
}

.step-content h3 {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 8px;
}

.step-content p {
    font-size: 15px;
    color: var(--text-secondary);
    line-height: 1.6;
}

.step-tools {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 16px;
}

.tool-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    padding: 6px 12px;
    border-radius: 6px;
    font-size: 12px;
    color: var(--text-secondary);
}

.step-connector {
    display: flex;
    justify-content: center;
    padding: 0 24px;
    margin-left: 24px;
    color: var(--border);
}

.step-connector svg {
    width: 24px;
    height: 60px;
}

.section-cta {
    text-align: center;
    margin-top: 64px;
}

/* ============================================
   Why Section
   ============================================ */
.why-section {
    padding: 120px 0;
    background: var(--bg-card);
    border-top: 1px solid var(--border);
}

.tax-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    max-width: 800px;
    margin: 0 auto 56px;
}

.tax-item {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    background: var(--bg-dark);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 24px;
    transition: all 0.3s;
}

.tax-item:hover {
    border-color: rgba(248, 113, 113, 0.3);
}

.tax-icon {
    width: 44px;
    height: 44px;
    background: rgba(248, 113, 113, 0.1);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.tax-icon i {
    font-size: 18px;
    color: var(--danger);
}

.tax-text {
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.tax-text strong {
    font-size: 15px;
    font-weight: 600;
    color: var(--text-primary);
}

.tax-text span {
    font-size: 13px;
    color: var(--text-muted);
    line-height: 1.5;
}

.why-solution {
    text-align: center;
    max-width: 640px;
    margin: 0 auto 64px;
}

.why-solution p {
    font-size: 16px;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 12px;
}

.why-solution p strong {
    color: var(--text-primary);
}

.why-closer {
    font-size: 20px !important;
    margin-top: 8px;
}

.why-comparison {
    max-width: 800px;
    margin: 0 auto;
}

.comparison {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
}

.comparison-item {
    background: var(--bg-dark);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 28px;
}

.comparison-item.bad {
    border-color: rgba(248, 113, 113, 0.3);
}

.comparison-item.good {
    border-color: rgba(74, 222, 128, 0.3);
    background: linear-gradient(180deg, rgba(74, 222, 128, 0.05) 0%, transparent 50%);
}

.comparison-header {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 16px;
    font-weight: 600;
    margin-bottom: 20px;
    padding-bottom: 16px;
    border-bottom: 1px solid var(--border);
}

.comparison-icon {
    font-size: 18px;
    color: var(--danger);
}

.comparison-logo {
    flex-shrink: 0;
}

.comparison-item ul {
    list-style: none;
}

.comparison-item li {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    font-size: 14px;
    color: var(--text-secondary);
    margin-bottom: 12px;
    line-height: 1.5;
}

.comparison-item li:last-child {
    margin-bottom: 0;
}

.comparison-item li i {
    margin-top: 3px;
    flex-shrink: 0;
    font-size: 12px;
}

.comparison-item.bad li i {
    color: var(--danger);
}

.comparison-item.good li i {
    color: var(--success);
}

/* ============================================
   Human QA Section
   ============================================ */
.human-qa {
    padding: 80px 0;
    border-top: 1px solid var(--border);
}

.human-qa-card {
    display: flex;
    align-items: flex-start;
    gap: 40px;
    max-width: 800px;
    margin: 0 auto;
    background: var(--bg-dark);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 48px;
}

.human-qa-icon {
    width: 64px;
    height: 64px;
    background: linear-gradient(135deg, rgba(251, 191, 36, 0.1) 0%, rgba(251, 146, 60, 0.1) 100%);
    border: 1px solid rgba(251, 191, 36, 0.3);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.human-qa-icon i {
    font-size: 24px;
    color: #fbbf24;
}

.human-qa-tag {
    display: inline-block;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: #fbbf24;
    margin-bottom: 12px;
}

.human-qa-content h3 {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 12px;
}

.human-qa-content > p {
    font-size: 15px;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 20px;
}

.human-qa-list {
    list-style: none;
    margin-bottom: 24px;
}

.human-qa-list li {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 14px;
    color: var(--text-secondary);
    margin-bottom: 10px;
}

.human-qa-list li i {
    color: #fbbf24;
    font-size: 12px;
}

.btn-human-qa {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: transparent;
    border: 1px solid rgba(251, 191, 36, 0.4);
    color: #fbbf24;
    font-size: 14px;
    font-weight: 600;
    padding: 12px 24px;
    border-radius: 10px;
    text-decoration: none;
    transition: all 0.3s;
}

.btn-human-qa:hover {
    background: rgba(251, 191, 36, 0.1);
    border-color: #fbbf24;
    transform: translateY(-2px);
}

.btn-human-qa i {
    font-size: 12px;
    transition: transform 0.3s;
}

.btn-human-qa:hover i {
    transform: translateX(4px);
}

/* ============================================
   Bottom CTA Section
   ============================================ */
.bottom-cta {
    padding: 120px 0;
    position: relative;
    text-align: center;
}

.bottom-cta::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 600px;
    height: 400px;
    background: var(--accent);
    filter: blur(200px);
    opacity: 0.06;
    pointer-events: none;
    border-radius: 50%;
}

.bottom-cta-content {
    position: relative;
    z-index: 1;
}

.bottom-cta h2 {
    font-size: 36px;
    font-weight: 700;
    letter-spacing: -1px;
    margin-bottom: 16px;
}

.bottom-cta p {
    font-size: 17px;
    color: var(--text-secondary);
    margin-bottom: 32px;
}

/* ============================================
   Shared Form Styles (used in modal)
   ============================================ */
.ea-form {
    max-width: 420px;
    margin: 0 auto 24px;
}

.ea-input-group {
    display: flex;
    gap: 12px;
}

.ea-input-group input {
    flex: 1;
    background: var(--bg-dark);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 14px 18px;
    font-size: 15px;
    color: var(--text-primary);
    font-family: var(--font-main);
    outline: none;
    transition: border-color 0.2s;
}

.ea-input-group input::placeholder {
    color: var(--text-muted);
}

.ea-input-group input:focus {
    border-color: var(--accent);
}

.btn-submit {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: var(--gradient);
    color: var(--bg-dark);
    font-size: 15px;
    font-weight: 600;
    padding: 14px 28px;
    border-radius: 10px;
    border: none;
    cursor: pointer;
    transition: all 0.3s;
    white-space: nowrap;
}

.btn-submit:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px var(--accent-glow);
}

.ea-message {
    font-size: 14px;
    margin-top: 12px;
    min-height: 20px;
}

.ea-message.success {
    color: var(--success);
}

.ea-message.error {
    color: var(--danger);
}

.ea-demo-link {
    font-size: 14px;
    color: var(--text-muted);
}

.ea-demo-link a {
    color: var(--accent);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s;
}

.ea-demo-link a:hover {
    color: var(--text-primary);
}

/* ============================================
   Footer
   ============================================ */
.footer {
    padding: 64px 0 32px;
    background: var(--bg-card);
    border-top: 1px solid var(--border);
}

.footer-main {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 64px;
    margin-bottom: 48px;
}

.footer-brand {
    max-width: 300px;
}

.footer-brand .logo {
    margin-bottom: 16px;
}

.footer-brand p {
    font-size: 14px;
    color: var(--text-muted);
    line-height: 1.6;
}

.footer-links {
    display: flex;
    gap: 64px;
}

.footer-col h4 {
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--text-secondary);
    margin-bottom: 16px;
}

.footer-col a {
    display: block;
    font-size: 14px;
    color: var(--text-muted);
    text-decoration: none;
    margin-bottom: 12px;
    transition: color 0.2s;
}

.footer-col a:hover {
    color: var(--text-primary);
}

.footer-bottom {
    padding-top: 32px;
    border-top: 1px solid var(--border);
    text-align: center;
}

.footer-bottom p {
    font-size: 13px;
    color: var(--text-muted);
}

/* ============================================
   Responsive
   ============================================ */
@media (max-width: 1024px) {
    .hero .container {
        grid-template-columns: 1fr;
        text-align: center;
    }
    
    .hero-content {
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    
    .hero-sub {
        max-width: 600px;
    }
    
    .hero-cta {
        justify-content: center;
    }
    
    .hero-stats {
        justify-content: center;
    }
    
    .hero-visual {
        order: -1;
        margin-bottom: 40px;
    }
    
    .terminal {
        max-width: 480px;
    }
    
    .pillar-card {
        width: calc(50% - 10px);
    }
    
    .tax-grid {
        grid-template-columns: 1fr 1fr;
    }

    .comparison {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .nav-menu {
        display: none;
    }
    
    .mobile-menu-btn {
        display: flex;
    }
    
    .hero h1 {
        font-size: 40px;
        letter-spacing: -1px;
    }
    
    .hero h1 br {
        display: none;
    }
    
    .section-header h2 {
        font-size: 32px;
    }
    
    .pillar-card {
        width: calc(50% - 10px);
    }
    
    .hero-stats {
        flex-wrap: wrap;
        gap: 24px;
    }
    
    .stat-divider {
        display: none;
    }

    .tax-grid {
        grid-template-columns: 1fr;
    }

    .comparison {
        grid-template-columns: 1fr;
    }
    
    .footer-main {
        grid-template-columns: 1fr;
        gap: 48px;
    }
    
    .footer-links {
        gap: 32px;
        flex-wrap: wrap;
    }
    
    .ea-input-group {
        flex-direction: column;
    }
    
    .human-qa-card {
        flex-direction: column;
        padding: 32px 24px;
        text-align: center;
    }
    
    .human-qa-icon {
        margin: 0 auto;
    }
    
    .human-qa-list li {
        justify-content: center;
    }
    
    .human-qa-content h3 {
        font-size: 22px;
    }

    .why-text h2 {
        font-size: 30px;
    }

    .why-text h2 br {
        display: none;
    }
}

@media (max-width: 480px) {
    .hero {
        padding: 100px 0 60px;
    }
    
    .hero h1 {
        font-size: 32px;
    }
    
    .hero-sub {
        font-size: 16px;
    }
    
    .hero-cta {
        flex-direction: column;
        width: 100%;
    }
    
    .btn-primary,
    .btn-ghost {
        width: 100%;
        justify-content: center;
    }
    
    .pillar-card {
        width: 100%;
    }
    
    .section-header h2 {
        font-size: 28px;
    }
    
    .why-text h2 {
        font-size: 26px;
    }
    
    .bottom-cta h2 {
        font-size: 28px;
    }

    .modal-card {
        padding: 32px 20px;
    }
}
</style>

<script>
// Modal functionality
const modal = document.getElementById('ea-modal');
const modalClose = document.getElementById('modal-close');

document.querySelectorAll('[data-ea-trigger]').forEach(btn => {
    btn.addEventListener('click', (e) => {
        e.preventDefault();
        modal.classList.add('active');
        // Focus the email input after modal opens
        setTimeout(() => {
            const emailInput = modal.querySelector('input[type="email"]');
            if (emailInput) emailInput.focus();
        }, 100);
    });
});

modalClose.addEventListener('click', () => {
    modal.classList.remove('active');
});

modal.addEventListener('click', (e) => {
    if (e.target === modal) {
        modal.classList.remove('active');
    }
});

// Close modal on Escape key
document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && modal.classList.contains('active')) {
        modal.classList.remove('active');
    }
});

// Smooth scroll for non-modal links
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    if (anchor.hasAttribute('data-ea-trigger')) return;
    anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
    });
});

// Newsletter subscription
document.getElementById('subscribe-form').addEventListener('submit', async function(e) {
    e.preventDefault();
    const form = this;
    const email = form.email.value;
    const message = document.getElementById('subscribe-message');
    const button = form.querySelector('button');
    const originalHTML = button.innerHTML;
    
    button.disabled = true;
    button.innerHTML = '<span>Sending...</span>';
    
    try {
        const response = await fetch('/subscribe', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email })
        });
        
        const data = await response.json();
        
        if (data.success) {
            message.textContent = '\u2713 You\'re on the list! We\'ll be in touch soon.';
            message.className = 'ea-message success';
            form.email.value = '';
        } else {
            message.textContent = data.error || 'Something went wrong. Please try again.';
            message.className = 'ea-message error';
        }
    } catch (error) {
        message.textContent = 'Something went wrong. Please try again.';
        message.className = 'ea-message error';
    }
    
    button.disabled = false;
    button.innerHTML = originalHTML;
});

// Mobile menu toggle
const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
const navMenu = document.querySelector('.nav-menu');

if (mobileMenuBtn) {
    mobileMenuBtn.addEventListener('click', () => {
        navMenu.classList.toggle('active');
    });
}
</script>

<!-- Crisp Chat -->
<script type="text/javascript">
    window.$crisp=[];
    window.CRISP_WEBSITE_ID="ca41fe4c-d770-4c1d-94f4-0fcc32a7067f";
    (function(){
        d=document;
        s=d.createElement("script");
        s.src="https://client.crisp.chat/l.js";
        s.async=1;
        d.getElementsByTagName("head")[0].appendChild(s);
    })();
</script>

<!-- DataFast Analytics -->
<script defer data-website-id="68b5d8fdc45c08d68dd53e66" data-domain="www.firstqa.dev" src="https://datafa.st/js/script.js"></script>

</body>
</html>
