<nav class="navbar">
  <div class="container">
    <a href="/dashboard" class="logo">
      <span class="logo-icon">O</span>
      <span>FirstQA</span>
    </a>
    <div class="progress-bar" role="progressbar">
      <% for (var i = 1; i <= 5; i++) { %>
        <span class="progress-step <%= i <= (typeof progress !== 'undefined' ? progress : 1) ? 'active' : '' %>"></span>
      <% } %>
    </div>
  </div>
</nav>
